# Ledger Rules

The ledger is append-only. Every transaction is idempotent, currency-homogeneous, and balanced: total debits equal total credits. Historical entries are never edited; reversals and adjustments create new transactions. Holds are liabilities/reservations, not charges. Balances are rebuildable snapshots.

