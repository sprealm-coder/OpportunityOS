# Feature Lifecycle

Feature definitions are persistent contracts. Tenant and brand grants select release channels and can be blocked by capability or risk gates. Disabled features return `feature_not_enabled`; their data, migrations, API contracts, and audit history remain intact.

