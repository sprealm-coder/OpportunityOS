[project]
name = "opportunity-intelligence-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.11,<3", "httpx>=0.28,<1"]

[project.optional-dependencies]
test = ["pytest>=8.4,<9"]

[tool.pytest.ini_options]
pythonpath = ["src"]

